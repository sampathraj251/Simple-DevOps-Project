apiVersion: v1
kind: Service
metadata:
  name: sampath-service
  labels:
    app: sampath-devops-project
spec:
  selector:
    app: sampath-devops-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200
